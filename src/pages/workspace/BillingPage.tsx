import { UnderConstruction } from '@/components/Common';

/**
 * Usage (mục 5.2): Tenant Admin chỉ xem gói hiện tại và quota. Chưa có API
 * plan/quota nên dùng placeholder, không giả lập gói/hạn mức như dữ liệu thật.
 */
export default function BillingPage() {
  return <UnderConstruction />;
}
